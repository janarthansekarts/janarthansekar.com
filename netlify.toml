[build]
command = "composer install --no-dev --prefer-dist --no-interaction && npm install && npm run build"
publish = "build_production"

[build.environment]
PHP_VERSION = "8.2"
NODE_VERSION = "18"